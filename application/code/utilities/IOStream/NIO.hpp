#pragma once

#include <ncurses.h>
#include <string>

namespace NI
{
    extern int getline(std::string& input);
    extern void trimWhiteSpaces(std::string& uglyString);
} // namespace NI

namespace NO
{ 
    
} // namespace NO
